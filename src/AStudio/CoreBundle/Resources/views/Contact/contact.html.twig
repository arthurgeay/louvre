{% extends "AStudioCoreBundle::layout.html.twig" %}

{% block title %} Contact - {{parent()}} {% endblock %}

{% block body %}
<div class="page-header page-header-small clear-filter" filter-color="orange">
    <div class="page-header-image" data-parallax="true" style="background-image: url('{{ asset('img/louvre.jpeg') }}');">
    </div>
    <div class="container">
      <div class="content-center">
        <div class="text-center">
          <h1 class="title">Contact</h1>
        </div>
      </div>
    </div>
  </div>
  <div class="section">
<div class="container">
	<div class="centered">
	<div class="col-md-6">
	 {% for flashMessage in app.session.flashbag.get('success') %}
        <div class="alert alert-success" role="alert">
        	<div class="container">
        		<div class="alert-icon">
        			<i class="now-ui-icons ui-2_like"></i>
        		</div>
            	{{ flashMessage }}
            	<button type="button" class="close" data-dismiss="alert" aria-label="Close">
            		<span aria-hidden="true">
            			<i class="now-ui-icons ui-1_simple-remove"></i>
            		</span>
            	</button>
        	</div>
        </div>
    {% endfor %}
</div>
</div>""

    	<div class="well">
	    	{{ form_start(form) }}
	    		{{ form_errors(form) }}

				<div class="form-group centered">
				<div class="col-md-6">
				{% if form.lastname.vars.errors|length > 0 %}
        			<div class="alert alert-danger">
        				{{ form_errors(form.lastname) }}
        			</div>
        		{% endif %}

					{{ form_widget(form.lastname, {'attr': {'class': 'form-control', 'placeholder': 'Nom'}})}}
	
				</div>
			</div>

				<div class="form-group centered">
				<div class="col-md-6">
				{% if form.firstname.vars.errors|length > 0 %}
        			<div class="alert alert-danger">
        				{{ form_errors(form.firstname) }}
        			</div>
        		{% endif %}

				
					{{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'placeholder': 'Prénom'}})}}
				</div>
				</div>

				<div class="form-group centered">
				<div class="col-md-6">
				{% if form.email.vars.errors|length > 0 %}
        			<div class="alert alert-danger">
        				{{ form_errors(form.email) }}
        			</div>
        		{% endif %}

				
					{{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Adresse e-mail'}})}}
				</div>
				</div>

				<div class="form-group centered">
					<div class="col-md-6">

				{% if form.message.vars.errors|length > 0 %}
        			<div class="alert alert-danger">
        				{{ form_errors(form.message) }}
        			</div>
        		{% endif %}

				
					{{ form_widget(form.message, {'attr': {'class': 'form-control', 'placeholder': 'Votre message'}})}}
				</div>
				</div>

				<div class="form-group centered">
          			<div class="col-md-6">
            		<button type="submit" class="btn btn-primary btn-round btn-lg btn-block"><i class="fa fa-paper-plane" aria-hidden="true"></i> Envoyer votre message</button>
          			</div>
          
      			</div>

				{{ form_rest(form) }}
	    	{{ form_end(form) }}
    	</div>
 </div> 
</div>

{% endblock %}